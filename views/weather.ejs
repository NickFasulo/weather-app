<!DOCTYPE html>
<html>
  <head>
    <meta
      name="viewport"
      content="initial-scale=1,maximum-scale=1,user-scalable=no"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.3.1/leaflet.css"
    />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.3.1/leaflet.js"></script>
    <title>Weather Now</title>
  </head>
  <body>
    <% include ./nav %>
    <div id="map"></div>
    <script>
      const map = L.map('map').setView([<%= items.lat %>, <%= items.lon %>], 13);
      L.tileLayer(
        'https://api.maptiler.com/maps/streets/{z}/{x}/{y}.png?key=9hwct2iyimE8zg3KfMSF',
        {
          tileSize: 512,
          zoomOffset: -1,
          minZoom: 1,
          attribution:
            '<a href="https://www.maptiler.com/copyright/" target="_blank">© MapTiler</a> <a href="https://www.openstreetmap.org/copyright" target="_blank">© OpenStreetMap contributors</a>',
          crossOrigin: true
        }
      ).addTo(map);

      const latlng = L.latLng(<%= items.lat %>, <%= items.lon %>);

      const popup = L.popup()
        .setLatLng(latlng)
        .setContent('<div class="popup"><h3 id="location"><%= items.city %>, <%= items.state %></h3><h5></h5><div class="weather"><img id="icon" src="https://www.weatherbit.io/static/img/icons/<%= items.icon %>.png" alt="..."><br /><div class="temps"><p id="temp"><%= items.temp %>&deg;</p>&nbsp;&nbsp;<p id="app-temp"><%= items.appTemp %>&deg;</p></div></div></div>')
        .openOn(map);
    </script>
  </body>
</html>
